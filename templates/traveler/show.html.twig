{% extends 'base.html.twig' %}

{% block title %}Traveler{% endblock %}

{% block body %}
    <h1>Traveler</h1>

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traveler.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ traveler.name }}</td>
            </tr>
            <tr>
                <th>Num_pass</th>
                <td>{{ traveler.numPass }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_traveler_index') }}">back to list</a>

    <a href="{{ path('app_traveler_edit', {'id': traveler.id}) }}">edit</a>

    {{ include('traveler/_delete_form.html.twig') }}
{% endblock %}
