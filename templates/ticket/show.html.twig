{#
{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ ticket.number }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ ticket.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('app_ticket_edit', {'id': ticket.id}) }}" class="btn btn-success">edit</a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
#}
{% extends "dashboard/adminbase.html.twig" %}
{% block title %}Admin Dashboard - Utilisateurs
{% endblock %}
{% block AdminContent %}
    <div class="content-wrapper">
        <div class="container-xxl flex-grow-1 container-p-y">
            <div class="card mb-4">
                <div class="card-header d-flex align-items-center justify-content-between">
                    <h5 class="mb-0">Modifier utilisateur</h5>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ ticket.id }}</td>
                        </tr>
                        <tr>
                            <th>Number</th>
                            <td>{{ ticket.number }}</td>
                        </tr>
                        <tr>
                            <th>Price</th>
                            <td>{{ ticket.price }}</td>
                        </tr>
                        </tbody>
                    </table>
                    <a href="{{ path('app_ticket_index') }}" class="btn btn-primary">back to list</a>

                    <a href="{{ path('app_ticket_edit', {'id': ticket.id}) }}" class="btn btn-success">edit</a>

                    {{ include('ticket/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}

